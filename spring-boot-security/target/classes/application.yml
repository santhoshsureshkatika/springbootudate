server:
  port: 4444

spring:
  datasource:
    password: Temp1234
    url: jdbc:mysql://localhost:7780/javabatch
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        database-platform: org.hibernate.dialect.MySQL5Dialect
        
  #security:
  # user:
    #  name: Santu
   #   password: Santu
      